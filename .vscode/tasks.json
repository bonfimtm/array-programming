{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Export to HTML",
            "type": "shell",
            "command": "${cwd}/venv/bin/jupyter-nbconvert --to slides src/array_programming.ipynb",
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}